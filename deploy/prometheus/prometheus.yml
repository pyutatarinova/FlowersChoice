global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "flowerschoice-backend"
    metrics_path: /internal/metrics
    static_configs:
      - targets:
          - backend:3001
